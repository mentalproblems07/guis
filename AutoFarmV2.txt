-- Declare a 'lib' variable and load the library.
local lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/AZYsGithub/Arceus-X-UI-Library/main/source.lua"))()

-- Set the title
lib:SetTitle("AutoFarm GUI by Bloodfallen") -- Note: you can use rich text as well.

-- Set the icon
lib:SetIcon("http://www.roblox.com/asset/?id=9178187770") -- Set the icon, with roblox asset id or anything that valid. You also able to use getcustomasset() to load custom image.

-- Functions
-- Makes a button, prints "Clicked!" while clicked.
local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")

local player = Players.LocalPlayer
local userId = player.UserId

local thumbType = Enum.ThumbnailType.HeadShot
local thumbSize = Enum.ThumbnailSize.Size48x48
local content, isReady = Players:GetUserThumbnailAsync(userId, thumbType, thumbSize)

StarterGui:SetCore("SendNotification", {
	Title = "Hoşgeldin " .. player.Name .. "!",
	Text = "Powered by JITEM!",
	Icon = content
})


-- Makes a toggle, prints "On!" while switched on, "Off!" while turned off.
lib:AddToggle("Anti-Admin", function(state)
	if state then
		getgenv().antiMod = state
		if getgenv().antiMod then
			local rolesToKick = {"💎｜Special Member", "⚡｜Moderator", "✨｜Admin", "👾｜Dev", "👑｜Creator"}

			local function onPlayer(player)
				local success, role = pcall(function()
					return player:GetRoleInGroup(15762035)
				end)

				if success and (table.find(rolesToKick, role) or player:IsInGroup(16817874)) then
					Players.LocalPlayer:Kick(player.Name .. " joined.")
				end
			end

			game.Players.PlayerAdded:Connect(onPlayer)
			for _, player in ipairs(Players:GetPlayers()) do
				if player ~= Players.LocalPlayer then
					onPlayer(player)
				end
			end
		end
	else
		print('Off!') -- Prints 'Off!', feel free to replace the code.
	end
end, false) -- 'true' or 'false', to make it default, if it's 'true' it will toggled on by default and vice versa.




lib:AddToggle("Farm FS", function(state)
	if state then
		getgenv().farming = true
		spawn(function()
			while getgenv().farming do
				task.wait(0.1)
				local args = {
					[1] = "Add_FS_Request"
				}
				game.ReplicatedStorage.RemoteEvent:FireServer(args)
			end
		end)
	else
		getgenv().farming = false
		print('Off!')
	end
end, false)


lib:AddToggle("Farm Speed", function(state)
	if state then
		getgenv().farmspeed = true
		spawn(function()
			while getgenv().farmspeed do
				task.wait(0.1)		
				game.ReplicatedStorage:WaitForChild("RemoteEvent"):FireServer({[1] = "Add_MS_Request"})
			end
		end)
	else
		getgenv().farmspeed = false
		print('Off!')
	end
end, false)

lib:AddToggle("Farm Jump", function(state)
	if state then
		getgenv().farmjump = true
		spawn(function()
			while getgenv().farmjump do					
				game.ReplicatedStorage:WaitForChild("RemoteEvent"):FireServer({[1] = "Add_JF_Request"})
				task.wait(0.1)
			end
		end)	
	else
		getgenv().farmjump = false
		print('Off!')
	end
end, false)



lib:AddComboBox("Teleport Menusu", {"FS 10T","BT 10T","PP 1QA"}, function(selection)
	if selection == "FS 10T" then -- This is an example, you might modify a little bit.
		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-369, 15735, -9)
	elseif selection == "BT 10T" then
		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-279, 279, 1007)
	elseif selection == "PP 1QA" then
		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-2544, 5412, -495)
	end
end)


lib:AddButton("Respawn", function()
	game.ReplicatedStorage:WaitForChild("RemoteEvent"):FireServer({[1] = "Respawn"})
end)


lib:AddButton("Infinite Yield", function()
	loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
end)

lib:AddButton("Anti-AFK", function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/suji0n/Rice-Anti-Afk/main/Wind", true))()
end)

lib:AddButton("Eski BlackHub", function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/de07nz15/Spts-Gui-Scripts/refs/heads/main/BH1", true))()
end)


